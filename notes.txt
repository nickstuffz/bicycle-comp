API Design:
    Endpoints:
        READ(partName)
        Response: {
            partName:
            partNameLink:
            shifter: 
            rear-derailleur:
            front-derailleur:
            cassette:
            chain:
        }

Todo:
Implement BLOCK data into tables somewhere
Start with 1 page only
User authenticated build page
Look into indexing tables

Reminder:
Try excluding same pairs (pod1,pod1), as I think the SQL implementation of the query handles keeping the original pods

https://sqlformat.darold.net/
